<?xml version="1.0" encoding="utf-8"?>
<!-- Close circle — soft red tint with thin border (handoff). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/hg_close_bg" />
    <stroke android:width="1dp" android:color="@color/hg_close_border" />
</shape>
