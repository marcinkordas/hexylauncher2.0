<?xml version="1.0" encoding="utf-8"?>
<!-- Active tab fallback (used by EditModeOverlay.setMode for backward compatibility). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="16dp" />
    <solid android:color="@color/hg_tab_active_bg" />
    <stroke android:width="1dp" android:color="@color/hg_tab_active_border" />
</shape>
